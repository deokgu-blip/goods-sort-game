{
  "perspective": 560,
  "eyeY": 0.20,
  "sideReveal": 0.5,
  "wallOuterReveal": 0.5,
  "wallCenterReveal": 0,
  "wallFalloff": 1.0,
  "depth": 0.68,
  "floorDepth": 1.0,
  "frameCeilingDepth": 0,
  "shelfAngle": 90,
  "cameraAngle": 0,
  "cameraHeight": 0.5,
  "postThickness": 9,
  "frameThickness": 12,
  "ceilingVisibility": 0,
  "cornerRadius": 0,
  "goodFillHeight": 0.88,
  "cubbyAspect": 0.95,
  "rimH": 7,
  "goodGap": 0.04,
  "lightAngle": 315,
  "shadowStrength": 0.45,
  "cubbyShadowStrength": 0.45,
  "goodShadowStrength": 0.45,
  "backDim": 0.4,
  "topShadow": 0.5,
  "gridColor": "#caa06a",
  "_note": "Shelf 3D look config = single source of truth. Defaults are baked INLINE in engine/goods-sort.html as SHELF_DEFAULTS (the single self-contained HTML works offline without this file). The engine also merges an override from localStorage['gs_shelf_config'] (the editor's save) on top of the defaults. This JSON mirrors the current defaults for reference/commit. To change the game's default look: edit it live in editor/shelf3d.html, click '설정 복사' (or Export .json), and paste the JSON into BOTH SHELF_DEFAULTS in the engine AND this file.",
  "_units": {
    "perspective": "px — per-niche perspective distance (작을수록 강한 원근/입체)",
    "eyeY": "fraction of cubby height — PER-CUBBY perspective-origin Y (0.5=정중앙, <0.5=위에서 내려다봐 바닥 더 보임). 0.20=look-down, 바닥면 보임. (전역 카메라 높이는 cameraHeight.)",
    "sideReveal": "V20 — BACK-COMPAT ALIAS for wallOuterReveal. A legacy config with only sideReveal migrates into wallOuterReveal (the OUTER side-wall reveal).",
    "wallOuterReveal": "V20 — 0..1.3 mult on the OUTER (center-away, '중앙을 등지는') side-wall depth. Default 0.5 (= old sideReveal). Scaled per-cubby by the center-distance falloff (wallFalloff): center column ≈ 0, edge columns the OUTER wall sliver.",
    "wallCenterReveal": "V20 — 0..1.3 mult on the CENTER-FACING (toward board center, '중앙을 바라보는') side-wall depth. Default 0 = hidden (current OUTER-only look). Each cubby's two side walls are INDEPENDENT.",
    "wallFalloff": "V20 — 0.3..3 exponent: per-cubby factor = distFrac^wallFalloff (distFrac = |cubbyCx−boardCx| / (gridW/2)). Higher = walls vanish FASTER toward center ('중앙으로 올수록 벽면이 얼마나 안보일지'). Center column ≈ 0.",
    "depth": "fraction of min(cubbyW,cubbyH) — BASE box depth D (선반 깊이/길이). floorDepth/frameCeilingDepth multiply this for those faces.",
    "floorDepth": "V20 — 0.2..1.5 multiplier on EACH cubby's FLOOR face depth ('각 그리드의 바닥면 깊이') = depth × floorDepth.",
    "frameCeilingDepth": "V21 — SINGLE intuitive ceiling control ('천장면 깊이/보이기'). 0 = ceiling HIDDEN (default, current look). >0 = ceiling SHOWS and DEEPENS; ceiling band depth = depth × frameCeilingDepth. No longer gated by ceilingVisibility (the old hard dependency made the depth slider a no-op).",
    "shelfAngle": "degrees — floor/ceiling fold angle (선반각도; 90=직각 박스, 작을수록 윗/아랫면 더 보임)",
    "cameraAngle": "V20 — degrees, GLOBAL board tilt (board-level rotateX on #shelf). Default 0 = upright (no backward lean). Distinct from the per-cubby eyeY.",
    "cameraHeight": "V20 — 0..1 GLOBAL camera height for the WHOLE board (board perspective-origin Y / #shelf transform-origin Y). 0.5 = centered.",
    "postThickness": "px — divider/post gap width between cubbies (기둥 두께)",
    "frameThickness": "px — outer cabinet wood frame thickness (프레임 두께)",
    "ceilingVisibility": "LEGACY — kept for back-compat only. V21 folds it into frameCeilingDepth (the single ceiling control): a legacy config with ceilingVisibility>0 (no frameCeilingDepth) migrates that visibility into frameCeilingDepth. Not used directly.",
    "cornerRadius": "px — frame & niche corner radius (0=각짐/ANGULAR)",
    "goodFillHeight": "fraction of cubby interior — good height (굿즈 크기)",
    "cubbyAspect": "shelf aspect-ratio is 1/cubbyAspect (cubby 가로:세로; <1=가로로 넓은 칸)",
    "rimH": "px — front-rim band thickness (앞 턱 나무 두께)",
    "goodGap": "fraction of cubby width — horizontal gap between adjacent goods (굿즈 간격). Each of the 3 front slots is a uniform cell (cubbyW/3); a good is scaled (real aspect preserved) to fit within cellW - goodGap*cubbyW and centered -> thin goods sit centered with side padding, NO overlap.",
    "lightAngle": "degrees — DIRECTIONAL light source angle (라이트 각도). 315=TOP-LEFT light -> shadows fall BOTTOM-RIGHT (dx>0,dy>0). Drives every shadow (good drop-shadows + each cubby's inner shading).",
    "shadowStrength": "LEGACY — the OLD single shadow param. V21 SPLITS it into cubbyShadowStrength + goodShadowStrength. A legacy config with only shadowStrength migrates it into BOTH. Kept for back-compat.",
    "cubbyShadowStrength": "V21 — 0..1 FRAME/CUBBY interior shading opacity (프레임/칸 그림자: .c-back inner shading + the shallow top band). Higher = darker cubby interiors. Independent of the goods' drop-shadow.",
    "goodShadowStrength": "V21 — 0..1 the GOODS' drop-shadow/contact opacity (굿즈 그림자). Higher = heavier shadows under the goods. Independent of the cubby shading.",
    "backDim": "fraction — back-row (peek) goods brightness (뒷줄 어둡기). 0.4 = VERY dark (deep interior shadow behind the front row, per reference); front goods stay full brightness. Depth-based (not cast-shadow): back/peek goods read dark regardless of a front good's presence.",
    "topShadow": "fraction — SHALLOW top inner-shadow strength (천장 그림자). A faint dark band along the TOP edge of each cubby interior (ceiling drop-shadow into the recess). Kept SHALLOW; actual opacity = cubbyShadowStrength * topShadow * 0.6.",
    "gridColor": "V21 — the SINGLE wood hue (그리드 나무 색, COLOR PICKER). Every wood face (outer frame rails/posts, shelf planks/floor, dividers, side walls, front rim, ceiling) is this SOLID color × a GRAYSCALE grain map, so the WHOLE shelf is ONE uniform tone and the picker recolors all faces identically (they differ only in a consistent per-face brightness). Default #caa06a = the current warm wood tone (preserves the default look)."
  }
}
